<t name="My Invoices and Payments" t-name="account.portal_my_invoices">
      <t t-call="portal.portal_layout" breadcrumbs_searchbar="True">

        <t t-call="portal.portal_searchbar" title.translate="Facturas"/>
        <t t-if="not invoices">
            <p class="alert alert-warning">Actualmente no hay facturas ni pagos para su cuenta.</p>
        </t>
        <t t-if="invoices" t-call="portal.portal_table">
            <thead>
                <tr class="active">
                    <th name="invoice_number">Factura #</th>
                    <th name="invoice_date">Fecha de factura</th>
                    <th name="due_date" class="d-none d-md-table-cell">Fecha de vencimiento</th>
                    <th name="amount_due" class="text-end pe-3">Importe adeudado</th>
                    <th name="status">Estado</th>
                </tr>
            </thead>
            <tbody>
                <t t-foreach="invoices" t-as="invoice_data">
                    <t t-set="invoice" t-value="invoice_data['invoice']"/>
                    <tr>
                        <td>
                            <a t-att-href="invoice.get_portal_url()" t-att-title="invoice.name">
                                <t t-out="invoice.name" t-if="invoice.name != '/'"/>
                                <em t-else="">Borrador de factura</em>
                            </a>
                        </td>
                        <td><span t-field="invoice.invoice_date"/></td>
                        <td class="d-none d-md-table-cell" t-att-class="'d-none d-md-table-cell text-danger' if invoice.invoice_date_due and invoice.invoice_date_due &lt; datetime.date.today() and invoice.payment_state in ['not_paid', 'partial'] else 'd-none d-md-table-cell'">
                            <span t-field="invoice.invoice_date_due"/>
                        </td>
                        <td name="invoice_amount" class="text-end pe-3"><span t-out="-invoice.amount_residual if invoice.move_type == 'out_refund' else invoice.amount_residual" t-options="{&quot;widget&quot;: &quot;monetary&quot;, &quot;display_currency&quot;: invoice.currency_id}"/></td>
                        <td name="invoice_status">
                            <t t-if="invoice.state == 'posted'" name="invoice_status_posted">
                                <span t-if="invoice.currency_id.is_zero(invoice.amount_residual)" class="badge rounded-pill text-bg-success">
                                    <i class="fa fa-fw fa-check" aria-label="Paid" title="Paid" role="img"/>
                                    <span class="d-none d-md-inline"> Pagado </span>
                                </span>
                                <span t-elif="invoice.payment_state == 'in_payment' and not invoice.currency_id.is_zero(invoice.amount_residual)" class="badge rounded-pill text-bg-info">
                                    <i class="fa fa-fw fa-check" aria-label="processing_payment" title="Processing Payment" role="img"/>
                                    <span class="d-none d-md-inline"> Procesando pago</span>
                                </span>
                                <span t-elif="invoice.payment_state == 'reversed'" class="badge rounded-pill text-bg-success">
                                    <i class="fa fa-fw fa-check" aria-label="Reversed" title="Reversed" role="img"/>
                                    <span class="d-none d-md-inline"> Revertido</span>
                                </span>
                                <span t-else="" class="badge rounded-pill text-bg-info" name="invoice_status_waiting_for_payment">
                                    <i class="fa fa-fw fa-clock-o" aria-label="Opened" title="Opened" role="img"/>
                                    <span class="d-none d-md-inline"> En espera del pago</span>
                                </span>
                            </t>
                            <t t-elif="invoice.state == 'cancel'">
                                <span class="badge rounded-pill text-bg-warning">
                                    <i class="fa fa-fw fa-remove" aria-label="Cancelled" title="Cancelled" role="img"/>
                                    <span class="d-none d-md-inline"> Cancelado</span>
                                </span>
                            </t>
                        </td>
                    </tr>
                </t>
            </tbody>
        </t>
      </t>
    </t>